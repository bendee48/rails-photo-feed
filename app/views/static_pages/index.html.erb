<h1>Photo Feed</h1>

<%= form_with url: root_path, method: :get, local: true do |f| %>
  <%= f.label 'flickr-id', 'Flickr ID' %>
  <%= f.text_field 'flickr-id' %>
  <%= f.submit 'Submit' %>
<% end %>

<%= @user_photos %>